# Kafka compatibility matrix

**Living document** for the optional Kafka wire shim (`--kafka-listen`).
Ship history: Phases **23–105** (git HEAD product). Binding deep dives:
`PHASE23_SPEC.md` … `PHASE105_SPEC.md`. Overview: [WHITEPAPER.md](./WHITEPAPER.md).
Semantic rows below describe **shipped** behavior.

## Enable

```bash
volant-server \
  --data-dir ./data \
  --listen 127.0.0.1:9092 \
  --kafka-listen 127.0.0.1:9093
```

Native Volant protocol remains on `--listen`. Shared-token Auth does **not**
apply on the Kafka port (use SASL or anonymous + ACLs).

## Supported APIs (current)

From `SUPPORTED_APIS` in `crates/volant-broker/src/kafka/mod.rs`:

| Key | API | Versions | Notes |
|----:|-----|----------|-------|
| 0 | Produce | 0–13 | Classic 0–8; flex v9+; TopicId v13; KIP-951 CurrentLeader v10+; txn 123 after timeout (Phase 94) |
| 1 | Fetch | 0–18 | Classic 0–11; flex v12–18; TopicId v13+; isolation LSO/aborted (Phase 86); ReplicaState v15+ (ReplicaId gates preferred redirect); NodeEndpoints v16+; CurrentLeader tag v12+; DivergingEpoch + sessions (Phase 88); omit-unchanged incremental (Phase 91); session idle TTL + max/LRU (Phase 95); durable local sessions (Phase 115); multi-broker session forward (Phase 119); best-effort peer mirror (Phase 138) + polish (139) + claim fence (143) + serve-from-mirror without promote (Phase 147 default; not Raft); PreferredReadReplica 126+133+140+144 |
| 2 | ListOffsets | 0–11 | Flex v6+; specials v7–11; READ_COMMITTED latest = LSO (Phase 86) |
| 3 | Metadata | 0–13 | Flex v9+; TopicId v10–13; top-level ErrorCode v13; live leader_epoch (Phase 87) |
| 8 | OffsetCommit | 0–10 | Flex v8+; TopicId v10; v6+ `committed_leader_epoch` stored (v0–5 / native → **-1**) |
| 9 | OffsetFetch | 0–10 | Flex v6+; multi-group v8; TopicId v10; RequireStable v7+ → **81**; v5+ returns stored `committed_leader_epoch` (legacy / unknown = **-1**) |
| 10 | FindCoordinator | 0–6 | Flex v3; batch v4–6; sticky murmur2 over static membership (Phase 121); txn Init-owner override; no share key_type; never TRANSACTION_ABORTABLE |
| 11 | JoinGroup | 0–9 | Flex v6+; ProtocolType/Reason/SkipAssignment v7–9 |
| 12 | Heartbeat | 0–4 | Flex v4 |
| 13 | LeaveGroup | 0–5 | Flex v4+; Reason v5 |
| 14 | SyncGroup | 0–5 | Flex v4+; ProtocolType/Name v5. Native **116/117** / key **14** apply assignment bytes when they decode; empty/garbage still peeks Join assignment (not join-set wait). |
| 15 | DescribeGroups | 0–6 | Flex v5; ErrorMessage v6 |
| 16 | ListGroups | 0–5 | Flex v3; StatesFilter v4; TypesFilter v5 (`classic`) |
| 17 | SaslHandshake | 0–1 | PLAIN, SCRAM-SHA-256, SCRAM-SHA-512 |
| 18 | ApiVersions | 0–5 | Flex v3–5; header always v0; empty feature tags; v5 ClusterId/NodeId ignored |
| 19 | CreateTopics | 0–7 | Flex v5+; TopicId response v7; assignment wait/rollback same as native (majority miss → **19**) |
| 20 | DeleteTopics | 0–6 | Flex v4+; ErrorMessage v5; TopicId v6; assignment wait/rollback same as native (majority miss → **19**) |
| 21 | DeleteRecords | 0–2 | Flex v2; GC/clip aborted soft markers vs log start (Phase 104/111); best-effort replica fan-out (Phase 113) + durable leader outbox retry for offline peers (Phase 116) + new-leader outbox reconcile on leadership change (Phase 123); optional journal majority wait via broker env `VOLANT_DELETE_RECORDS_WAIT_MAJORITY` (Phase 135 — fail → Kafka **19**); Volant **v2 request-level tag 0** = `wait_majority` u8 (0/1/2; **not** a Kafka standard field — librdkafka will not send it); v0–1 stay env/broker only |
| 22 | InitProducerId | 0–6 | Flex v2+; v6 Enable2Pc/KeepPreparedTxn (Phase 90 prepared MVP); OngoingTxn* when prepared |
| 23 | OffsetForLeaderEpoch | 0–4 | Flex v4; durable epoch history MVP (Phase 87); prior epochs → transition end |
| 24 | AddPartitionsToTxn | 0–5 | Flex v3; batch v4–5; 123 after timeout (Phase 94) |
| 25 | AddOffsetsToTxn | 0–4 | Flex v3+; v4 = v3 wire; may emit TRANSACTION_ABORTABLE (123) after timeout (Phase 94); multi-broker transparent forward to Init owner (Phase 122) |
| 26 | EndTxn | 0–5 | Flex v3+; v5 pid/epoch echo; 123 after timeout auto-abort (Phase 94); multi-broker transparent forward (Phase 120) |
| 27 | WriteTxnMarkers | 0–1 | Flex v1; replica-local COMMIT/ABORT control batches + soft `__txn_markers`; CoordinatorEpoch ignored; not EndTxn / not coordinator; Topic WRITE or Cluster ALTER (v0.250) |
| 28 | TxnOffsetCommit | 0–6 | Flex v3+; TopicId v6; v3+ generation/member fence (empty member or gen < 0 skip; instance ignored); 123 after timeout when no open (Phase 94); multi-broker transparent forward (Phase 122) |
| 29–31 | ACL admin | 0–3 | Flex v2+; User resource v3; LITERAL only; cluster Create/Delete **controller-only** + snapshot fan-out (Phase 113; **41** NotController) |
| 32 | DescribeConfigs | 0–4 | Flex v4; TOPIC + BROKER (Phase 99–103; name empty or local `node_id`; sparse durable; cluster effective values after Phase 113 push) |
| 33 | AlterConfigs | 0–2 | Flex v2; TOPIC + BROKER SET (empty = product default; name check Phase 103; sparse durable Phase 100/102; BROKER cluster Alter **controller-only** Phase 113 → **41**) |
| 34 | AlterReplicaLogDirs | 0–1 | Flex v1 (official Kafka flex is v2); parse and reject every move (**42**); single `data_dir`; files unmoved; Cluster ALTER or Topic ALTER; controller not required (v0.249) |
| 35 | DescribeLogDirs | 0–1 | Flex v1; local logs only; size = `Log::total_size`; offsetLag = LEO−HWM (0 if unknown); isFuture false; not multi-log.dirs |
| 36 | SaslAuthenticate | 0–2 | Flex v2 |
| 37 | CreatePartitions | 0–3 | Flex v2+; v3 = v2 wire (no KIP-599 quota); assignment wait/rollback same as native (majority miss → **19**) |
| 38 | CreateDelegationToken | 0 | Always flex (official Kafka flex is v2); no token store; **42** `INVALID_REQUEST` (`delegation tokens not supported`); Cluster ALTER; controller not required (v0.258) |
| 39 | RenewDelegationToken | 0 | Always flex (official Kafka flex is v2); no token store; **42** `INVALID_REQUEST` (`delegation tokens not supported`); expiry **-1**; Cluster ALTER; controller not required (v0.261) |
| 40 | ExpireDelegationToken | 0 | Always flex (official Kafka flex is v2); no token store; **42** `INVALID_REQUEST` (`delegation tokens not supported`); Cluster ALTER; controller not required (v0.260) |
| 41 | DescribeDelegationToken | 0 | Always flex residual (official first flex is 2); no token store; any owners filter → error **0**, empty tokens; Cluster DESCRIBE (v0.259) |
| 42 | DeleteGroups | 0–3 | Flex v2; ErrorMessage v3 |
| 43 | ElectLeaders | 0–1 | Classic 0; flex v1; preferred = `elect_leader(ISR∩live)`; unclean type 1 → **87**; TimeoutMs ignored; assignment wait/rollback same as reassign; not live copy / not `preferred.leader` |
| 44 | IncrementalAlterConfigs | 0–1 | SET/DELETE only; TOPIC + BROKER (Phase 99–103 name check + sparse durable; BROKER cluster Alter controller-only Phase 113) |
| 45 | AlterPartitionReassignments | 0 | Always flex; wraps native opcode 114 + assignment wait; TimeoutMs ignored; null replicas → **83** (no pending cancel log); not live copy |
| 46 | ListPartitionReassignments | 0 | Always flex; current assignment as `replicas`; empty `addingReplicas`/`removingReplicas`; TimeoutMs ignored; not live progress; no pending log |
| 47 | OffsetDelete | 0 | Classic only |
| 48 | DescribeClientQuotas | 0 | Always flex; no quota store; any filter → error **0**, empty entries; Cluster DESCRIBE (v0.241) |
| 49 | AlterClientQuotas | 0 | Always flex; no quota store; per-entry **42** `INVALID_REQUEST` (`quotas not supported`); nothing persisted; Cluster ALTER (v0.241) |
| 50 | DescribeUserScramCredentials | 0 | Always flex; wraps `ScramStore`; empty users = all; unknown user → **91**; Cluster DESCRIBE |
| 51 | AlterUserScramCredentials | 0 | Always flex; wraps `ScramStore`; upsert takes `saltedPassword` (not plaintext); Cluster ALTER |
| 52 | Vote | 0 | Always flex; not KRaft; parse and reject (**42**); empty topics; no throttleTimeMs; Cluster ALTER; controller not required (v0.270) |
| 53 | BeginQuorumEpoch | 1 | Always flex; not KRaft quorum epoch; parse and reject (**42**); empty topics; no throttleTimeMs; official v0 is classic — not advertised; Cluster ALTER; controller not required (v0.289) |
| 55 | DescribeQuorum | 0–1 | Always flex; wraps openraft leader/term/voters; not KRaft `__cluster_metadata`; empty topics → synthetic cluster partition 0; raft off → empty + **0**; cluster non-controller → **41**; Cluster DESCRIBE (v0.245) |
| 56 | AlterPartition | 0 | Always flex; wraps `apply_leader_isr_update`; official v0 TopicName + NewIsr `[]int32`; BrokerEpoch ignored; not KRaft NewIsrEpoch / ELR / DirectoryId; controller-only in cluster (**41**); single-node no-op **0**; Cluster ALTER (v0.257) |
| 57 | UpdateFeatures | 0–1 | Always flex; parse and reject every feature (**92**); nothing persisted; ApiVersions features stay empty; not KIP-584; Cluster ALTER; controller-only in cluster (**41**) |
| 58 | Envelope | 0 | Always flex; no request forwarding (not KIP-590); parse and reject (**42**); RequestData discarded; ResponseData null; no throttleTimeMs; Cluster ALTER; controller not required (v0.266) |
| 59 | FetchSnapshot | 0 | Always flex; not KRaft; parse and reject (**42**); empty topics; Cluster DESCRIBE; controller not required (v0.267) |
| 60 | DescribeCluster | 0–2 | Always flex; IsFenced always false |
| 61 | DescribeProducers | 0 | Always flex |
| 62 | BrokerRegistration | 0 | Always flex; not KRaft (no incarnation/DirectoryId/features); parse and reject (**42**); does not wrap AddBroker; overlay unchanged; Cluster ALTER; controller not required (v0.263) |
| 63 | BrokerHeartbeat | 0 | Always flex; not KRaft (no fencing/metadata offset/assigned epoch); parse and reject (**42**); does not wrap Heartbeat 12; overlay unchanged; Cluster ALTER; controller not required (v0.265) |
| 64 | UnregisterBroker | 0 | Always flex; wraps native `remove_broker` (v0.217 invert); not KRaft incarnation/DirectoryId; controller only (**41**); no cluster → **42**; TimeoutMs ignored; Cluster ALTER (v0.242) |
| 65 | DescribeTransactions | 0 | Always flex |
| 66 | ListTransactions | 0–2 | Pattern = simple `*` glob |
| 67 | AllocateProducerIds | 0 | Always flex; block of 1000 from `next_producer_id`; BrokerEpoch ignored; not KRaft; controller-only in cluster (**41**); Cluster ALTER (v0.246) |
| 68 | ConsumerGroupHeartbeat | 0 | Always flex; parse and reject (**42** `not KIP-848 consumer protocol`); does not wrap Heartbeat **12**; Group READ; controller not required (v0.269) |
| 69 | ConsumerGroupDescribe | 0 | Always flex; wraps `describe_group` (same snapshot as key **15**); not KIP-848 (`memberEpoch` **-1**, classic only); unknown → **69**; Group DESCRIBE (v0.264) |
| 70 | ControllerRegistration | 0 | Always flex; not a KRaft controller (no incarnation/ZK migration/listener store); parse and reject (**42**); does not wrap AddBroker; overlay unchanged; Cluster ALTER; controller not required (v0.268) |
| 71 | GetTelemetrySubscriptions | 0 | Always flex; no client telemetry (not KIP-714); error **0**, echo `clientInstanceId`, `subscriptionId = 0`, empty metrics, `pushIntervalMs = -1`; Cluster DESCRIBE (v0.253) |
| 72 | PushTelemetry | 0 | Always flex; no client telemetry (not KIP-714); parse and reject (**42**); nothing persisted; Cluster ALTER (v0.255) |
| 73 | AssignReplicasToDirs | 0 | Always flex; parse and reject every assignment (**42**); single `data_dir`; files unmoved; no DirectoryId storage; not KRaft; Cluster ALTER; controller not required (v0.251) |
| 74 | ListClientMetricsResources | 0 | Always flex; no client-metrics store; empty resources; Cluster DESCRIBE (v0.252) |
| 75 | DescribeTopicPartitions | 0 | Always flex; wraps Metadata (same leaders/ISR/epochs/TopicId); no ELR; simple `responsePartitionLimit` truncate; cursor start if topic is in the set else ignored (v0.237) |
| 76 | ShareGroupHeartbeat | 1 | Always flex; parse and reject (**42** `not KIP-932 share group`); does not wrap Heartbeat **12** or ConsumerGroupHeartbeat **68**; official v0 removed in Kafka 4.1; Group READ; controller not required (v0.275) |
| 77 | ShareGroupDescribe | 1 | Always flex; official validVersions **1** only (v0 EA removed in 4.1); not KIP-932; per-group **42** (`not KIP-932 share group`); empty members; no top-level error; Group DESCRIBE (v0.276) |
| 78 | ShareFetch | 1 | Always flex; parse and reject (**42** `not KIP-932 share fetch`); does not wrap Fetch **1**; empty Responses/NodeEndpoints; Group READ; controller not required (v0.277) |
| 79 | ShareAcknowledge | 1 | Always flex; parse and reject (**42** `not KIP-932 share acknowledge`); does not wrap OffsetCommit **8** / Fetch **1**; empty Responses / NodeEndpoints; Group READ; controller not required (v0.278) |
| 80 | AddRaftVoter | 0 | Always flex; not a KRaft raft voter (membership is overlay + native AddBroker); parse and reject (**42**); does not wrap AddBroker; overlay unchanged; Cluster ALTER; controller not required (v0.271) |
| 81 | RemoveRaftVoter | 0 | Always flex; not a KRaft raft voter (no voter set/DirectoryId); parse and reject (**42**); does not wrap remove_broker; overlay unchanged; Cluster ALTER; controller not required (v0.272) |
| 82 | UpdateRaftVoter | 0 | Always flex; not a KRaft voter set (no listener / KRaftVersionFeature store); parse and reject (**42**); overlay unchanged; Cluster ALTER; controller not required (v0.273) |
| 83 | InitializeShareGroupState | 0 | Always flex; not KIP-932 share-partition state; parse official v0 and reject per-partition (**42** `not KIP-932 share state`); no throttle / no top-level error; does not persist; does not wrap OffsetCommit; Group ALTER; controller not required (v0.279) |
| 84 | ReadShareGroupState | 0 | Always flex; not KIP-932 share-partition state; parse official v0 and reject per-partition (**42** `not KIP-932 share state`); no throttle / no top-level error; StateEpoch 0 / StartOffset -1 / empty StateBatches; does not persist; does not wrap OffsetFetch / OffsetCommit / InitializeShareGroupState; Group READ; controller not required (v0.280) |
| 85 | WriteShareGroupState | 0 | Always flex; not KIP-932 share-partition state; parse official v0 and reject per-partition (**42** `not KIP-932 share state`); no throttle / no top-level error; does not persist; does not wrap OffsetCommit / InitializeShareGroupState; Group ALTER; controller not required; official 0–1 (v1 DeliveryCompleteCount out of range) (v0.281) |
| 86 | DeleteShareGroupState | 0 | Always flex; not KIP-932 share-partition state; parse official v0 and reject per-partition (**42** `not KIP-932 share state`); no throttle / no top-level error; does not persist; does not wrap OffsetCommit / DeleteGroups / InitializeShareGroupState; Group ALTER; controller not required (v0.282) |
| 87 | ReadShareGroupStateSummary | 0 | Always flex; not KIP-932 share-partition state; parse official v0 (same shape as ReadShareGroupState 84) and reject per-partition (**42** `not KIP-932 share state`); no throttle / no top-level error; StateEpoch 0 / LeaderEpoch -1 / StartOffset -1; no DeliveryCompleteCount (v1+); does not persist; does not wrap OffsetFetch / OffsetCommit / InitializeShareGroupState / ReadShareGroupState; Group READ; controller not required (v0.283) |
| 88 | StreamsGroupHeartbeat | 0 | Always flex; not KIP-1071 streams groups; parse official v0 and reject (**42** `not KIP-1071 streams group`); official 0–1 (v1 TopologyDescriptionRequired / KIP-1331 out of range); does not wrap Heartbeat **12** / ConsumerGroupHeartbeat **68** / ShareGroupHeartbeat **76**; Group READ; controller not required (v0.285) |
| 89 | StreamsGroupDescribe | 0 | Always flex; official validVersions **0–1** (v1 IncludeTopologyDescription / KIP-1331 out of range); not KIP-1071; per-group **42** (`not KIP-1071 streams group`); empty members; Topology null; no top-level error besides throttle; Group DESCRIBE (v0.286) |
| 90 | DescribeShareGroupOffsets | 0 | Always flex; not KIP-932 share offsets; official validVersions **0–1** (v1 Lag / KIP-1226 out of range); per-group **42** (`not KIP-932 share offsets`) after empty Topics[]; throttle **0**; no top-level error; does not persist; does not wrap OffsetFetch **9** / describe_group / 69 / 77; Group DESCRIBE; controller not required (v0.284) |
| 91 | AlterShareGroupOffsets | 0 | Always flex; not KIP-932 share offsets; official validVersions **0** only; throttle **0**; top-level **and** per-partition **42** (`not KIP-932 share offsets`); echo topic/partition names; TopicId zeros; does not persist; does not wrap OffsetCommit **8** / DescribeShareGroupOffsets **90**; Group ALTER; controller not required (v0.287) |
| 92 | DeleteShareGroupOffsets | 0 | Always flex; not KIP-932 share offsets; official validVersions **0** only; top-level + per-topic **42** (`not KIP-932 share offsets`); throttle **0**; echo topic names + zero TopicId; does not persist; does not wrap OffsetCommit **8** / DeleteGroups **42** / OffsetDelete **47** / 90; Group ALTER; controller not required (v0.288) |
| 94 | UnregisterController | 0 | Always flex; not a KRaft controller (no unregister record); parse and reject (**42**); does not wrap `remove_broker`; overlay unchanged; Cluster ALTER; controller not required (v0.274) |

## Wire evolution (summary)

| Band | Phases | Theme |
|------|--------|-------|
| MVP | 23–27 | Produce/Fetch/Metadata/groups/admin surface |
| Formats | 28–34 | Compression, idempotence, SASL, SCRAM-512 |
| Classic max | 35–50 | Version ratchets for classic framing |
| Flexible | 51–66 | KIP-482 compact + modern admin |
| TopicId / modern | 67–85 | UUID topics, ListOffsets specials, KIP-890, KIP-951, group admin, CreatePartitions v3, FindCoordinator v5–6, AddOffsetsToTxn v4, ApiVersions 0–5, Fetch 0–18, ACL admin User resource v3 |
| READ_COMMITTED | 86 | Write-through txn + soft abort markers; true LSO; Fetch isolation filtering |
| Leader epochs | 87 | Durable OffsetForLeaderEpoch history MVP; Metadata live leader_epoch |
| Fetch sessions / DivergingEpoch | 88 | Sessions (create/forgotten/invalid); DivergingEpoch tag 0 on truncation; durable local Phase 115 |
| Control batches | 89 | EndTxn COMMIT/ABORT control RecordBatches on partition log (dual-write with soft markers) |
| Prepared 2PC MVP | 90 | Enable2Pc prepare-then-complete EndTxn; KeepPreparedTxn + OngoingTxn*; durable `__txn_prepared` |
| Omit-unchanged sessions | 91 | Empty-topics incremental omits partitions when HWM+LSO unchanged and records empty |
| Prepared timeout | 92 | Lazy auto-abort of prepared txns after configurable timeout (default 60s) |
| Open txn timeout | 93 | Honor InitProducerId `transaction_timeout_ms` (or broker default) for open write-through txns; lazy auto-abort |
| TRANSACTION_ABORTABLE | 94 | Honest subset: emit 123 after open/prepared timeout on Produce/EndTxn/Add*/TxnOffsetCommit; FindCoordinator never |
| Fetch session TTL / max | 95 | Idle TTL (default 60s) + max concurrent sessions (default 1000, LRU); lazy eviction |
| Durable fetch sessions | 115 | Per-broker `{data_dir}/__fetch_sessions`; restart restores session_id/epoch/omit cache within idle TTL |
| Multi-broker session handoff | 119 | Owner-encoded session_id + transparent Kafka Fetch forward (opcode 82/83); omit/epoch SoT remains owner while alive |
| Shared session mirror + promote | 138 | Best-effort peer MirrorPut/Delete (opcodes 90–93); foreign mirror table; put lag/fail still **70**; not Raft; no session_id re-encode |
| Session mirror polish | 139 | Coalesce dirty ops (one per session_id; Delete supersedes Put); debounced Puts (`VOLANT_FETCH_SESSION_MIRROR_PUT_MIN_INTERVAL_MS` default 50; Delete immediate); optional durable `__fetch_session_mirrors` (`VOLANT_FETCH_SESSION_MIRROR_DURABLE`); `mirror_gen` fence; still not Raft |
| Promote claim fence | 143 | `promoted_by` lowest-id on equal-fresh dual-promote; claim travels in MirrorPut JSON; metric `volant_fetch_session_promote_claim_reject_total`; brief dual primary until exchange; not Raft |
| Serve-from-mirror without promote | 147 | Owner miss + mirror → serve foreign mirror by default (no promote); metric `volant_fetch_session_serve_from_mirror_total`; promote opt-in via `VOLANT_FETCH_SESSION_PROMOTE_ON_MISS=1` or legacy `SERVE_MIRROR_WITHOUT_PROMOTE=0`; dual-epoch residual (not single SoT) |
| Preferred selector depth | 140 | Optional `VOLANT_PREFERRED_REPLICA_MAX_LEO_LAG` (unset = unlimited); `volant_preferred_replica_suppressed_total` on READ_COMMITTED suppress when candidate exists; not full Kafka selector/throttling |
| Preferred × session suppress | 144 | Suppress PreferredReadReplica when `req_session_id != 0` (non-FINAL); metric `volant_preferred_replica_session_suppressed_total`; full fetch `session_id==0` still prefers |
| Rack-aware create assignment | 145 | Multi-rack diversity on create/create-partitions; `VOLANT_RACK_AWARE_ASSIGNMENT` default on; metric `volant_rack_aware_assignment_total`; no rebalance |
| Transaction max timeout | 96 | Broker max (default 15m / `VOLANT_TRANSACTION_MAX_TIMEOUT_MS`); InitProducerId rejects over-max with **50**; effective open/prepared clamp |
| Background sweeper + metrics | 97 | Periodic open/prepared/session idle expiry (default 1s / `VOLANT_SWEEP_INTERVAL_MS`; `0` pauses bg); lazy paths remain; expired counters + open/prepared gauges |
| Crash≡abort control | 98 | Open→aborted promote appends ABORT control batches (dual-write) |
| Broker DescribeConfigs knobs | 99 | BROKER resource: `transaction.max.timeout.ms` + `volant.*` open/prepared/session/sweep; Alter + Incremental SET/DELETE |
| Durable broker config | 100 | Full snapshot of six knobs under `{data_dir}/__broker_config/state.json`; load after env; DELETE rewrites file |
| Graceful sweeper enable | 101 | Always spawn sweeper task; `0` pauses; `0→>0` via setter/Alter enables without process restart |
| Background shutdown join | 106 | `BackgroundTasks` stop+join for group/retention/sweeper/cluster loops; server drains on exit/signal |
| Accept drain + single-flight bg | 109 | Native/Kafka/metrics accept loops select shutdown + abort connections (bounded); `start_background_tasks` single-flight per broker |
| Soft-marker GC | 104 | DeleteRecords / retention / load drop aborted soft markers with `end_offset <= log_start`; persist `__txn_markers`; metric `volant_aborted_markers_gc_total` |
| Empty-AddPartitions control | 105 | AddPartitions membership tracked; EndTxn + crash promote append control for empty partitions (no fake soft ranges) |
| DescribeShareGroupOffsets reject | v0.284 | Key **90** v0 only (always flex); official 0–1 (v1 Lag / KIP-1226 out of range); per-group **42** after empty Topics[]; no persist / no OffsetFetch wrap |
| StreamsGroupHeartbeat reject | v0.285 | Key **88** v0 only (always flex); official 0–1 (v1 TopologyDescriptionRequired / KIP-1331 out of range); top-level **42**; no persist / no Heartbeat 12 / 68 / 76 wrap |
| StreamsGroupDescribe reject | v0.286 | Key **89** v0 only (always flex); official 0–1 (v1 IncludeTopologyDescription / KIP-1331 out of range); per-group **42**; empty members; Topology null; no classic describe wrap |
| AlterShareGroupOffsets reject | v0.287 | Key **91** v0 only (always flex); official 0 only; throttle + top-level **and** per-partition **42**; echo topic/partition names; no persist / no OffsetCommit wrap |
| DeleteShareGroupOffsets reject | v0.288 | Key **92** v0 only (always flex); official 0 only; top-level + per-topic **42**; echo topic names; no persist / no OffsetCommit / OffsetDelete wrap |
| BeginQuorumEpoch reject | v0.289 | Key **53** v1 only (always flex); official 0–1 (v0 classic, v1 flex); reject **42**; no throttle; empty topics; not openraft RequestVote / Vote 52 |

## Semantic honesty (open)

These are **current** product facts, not temporary docs lag:

| Area | Limitation |
|------|------------|
| Transactions | **Write-through** (Phase 86) + **control batches** (Phase 89/98/105) + **prepared 2PC MVP** (Phase 90) + **prepared timeout** (Phase 92) + **open timeout** (Phase 93) + **TRANSACTION_ABORTABLE subset** (Phase 94) + **max timeout clamp** (Phase 96) + **background sweeper** (Phase 97/101/106) + **broker config surface** (Phase 99) + **sparse durable restart** (Phase 100/102) + **BROKER name vs `node_id`** (Phase 103) + **soft-marker GC/clip** (Phase 104/111): data on log immediately; soft markers still SoT for LSO/aborted list; markers with `end_offset <= log_start` dropped on DeleteRecords / retention / load; straddlers clip `first_offset = log_start` (Phase 111; control batches on log not rewritten); open crash≡abort **with ABORT control batches** (Phase 98) including **empty AddPartitions membership** (Phase 105, control-only); prepared survives restart under `__txn_prepared` until complete or timeout auto-abort; EndTxn control batches on finalize for written **and** empty added partitions; open-txn honors InitProducerId `transaction_timeout_ms` (or `VOLANT_OPEN_TXN_TIMEOUT_MS` default 60s); broker max default **15m** (`VOLANT_TRANSACTION_MAX_TIMEOUT_MS`; `0` = no max) rejects over-max Init with **50** and clamps effective open/prepared; after timeout auto-abort Produce/EndTxn/Add*/TxnOffsetCommit may return **123** until EndTxn clears; open/prepared expiry runs lazy **and** on a background interval (default 1s / `VOLANT_SWEEP_INTERVAL_MS`; `0` = pause bg; always-spawn so 0→>0 without restart, Phase 101; graceful shutdown/join Phase 106); knobs Describe/Alter via BROKER resource (Phase 99); name must be empty or local `node_id` decimal else **42** (Phase 103); Alter persists **sparse** under `__broker_config` (Phase 100/102; only altered keys; DELETE unfreezes env) |
| 2PC | **MVP** (Phase 90/92/94/96/97/**114**/**120**/**121**/**122**/**124**/**127**/**128**): Enable2Pc → first EndTxn prepares; second matching EndTxn finalizes; KeepPreparedTxn + OngoingTxn*; prepared timeout auto-abort (default 60s, env `VOLANT_PREPARED_TXN_TIMEOUT_MS`, clamped by max); timeout → abortable mark → Kafka **123** (Phase 94); background + lazy expiry (Phase 97); **multi-broker** prepare/complete fan-out across partition leaders (Phase 114; controller cluster prepared index; inter-broker opcodes 76–81); **transparent EndTxn / AddOffsets / TxnOffsetCommit forward** to Init-owner coordinator when client hits another broker (Phase 120/122; opcodes 84/85; no dual prepare / dual offset buffer); **sticky FindCoordinator** for group/txn keys (Phase 121; murmur2 static ring + Init-owner override); **durable Init-owner registry** under `{data_dir}/__txn_coordinator` (Phase 124; restart restore) with **TTL GC** (Phase 127; default 24h / `VOLANT_TXN_COORDINATOR_TTL_MS`; `0` off) + **BROKER Describe/Alter** (Phase 128; `volant.txn.coordinator.registry.ttl.ms`); **not** full KIP-890/939 / Kafka `__transaction_state` topic |
| Epochs | **Durable history MVP** (Phase 87): `{data_dir}/__leader_epochs`; prior epochs return transition end offsets; Metadata advertises live epoch; not a full KRaft epoch state machine; Fetch **DivergingEpoch** on truncation (Phase 88) |
| TopicId | Deterministic UUID from Volant id (`volant` + zeros + u32), not KRaft random |
| Groups | Coordinator-driven assignment; GroupType always `classic`; states Empty / Stable / CompletingRebalance / PreparingRebalance. **ConsumerGroupDescribe (69)** is a classic-snapshot wrap of DescribeGroups — not KIP-848 (`memberEpoch` **-1**, no regex subscribe, no consumer protocol). **ConsumerGroupHeartbeat (68)** is advertised as a reject (**42**), not KIP-848 — clients must keep using classic Heartbeat **12**. **ShareGroupHeartbeat (76)** is advertised as a reject (**42**), not KIP-932. **StreamsGroupHeartbeat (88)** is advertised as a reject (**42**), not KIP-1071 — clients must keep using classic Heartbeat **12**. **ShareGroupDescribe (77)** is advertised as a per-group reject (**42**), not KIP-932 — clients must keep using DescribeGroups **15** / ConsumerGroupDescribe **69**. **StreamsGroupDescribe (89)** is advertised as a per-group reject (**42**), not KIP-1071 — clients must keep using DescribeGroups **15** / ConsumerGroupDescribe **69**. **DescribeShareGroupOffsets (90)** is advertised as a per-group reject (**42**), not KIP-932 share offsets — clients must keep using OffsetFetch **9**. **AlterShareGroupOffsets (91)** is advertised as a reject (**42**), not KIP-932 share offsets — clients must keep using OffsetCommit **8**. **DeleteShareGroupOffsets (92)** is advertised as a top-level + per-topic reject (**42**), not KIP-932 share offsets — clients must keep using OffsetDelete **47** / OffsetCommit **8**. Official Kafka 69 is the new consumer group API; Volant fills classic fields clients already have |
| Fetch sessions | **Real MVP** (Phase 88 + **91** + **95** + **97** + **99** + **100** + **102** + **103** + **115** + **119** + **126** + **138** + **139** + **143** + **144**): create/merge/forgotten; empty-topics re-fetch; errors 70/71; **omit-unchanged** when HWM+LSO unchanged and records empty; **idle TTL** (default 60s / `VOLANT_FETCH_SESSION_IDLE_MS`) + **max sessions** (default 1000 / `VOLANT_FETCH_SESSION_MAX`, LRU at cap); idle also background-swept (Phase 97); knobs on BROKER Describe/Alter (Phase 99) with **sparse** durable restart (Phase 100/102) and name vs `node_id` (Phase 103); **session table durable** under `{data_dir}/__fetch_sessions` (Phase 115); **multi-broker handoff** (Phase 119): cluster session_id embeds owner; non-owner transparent-forwards Fetch to owner over inter-broker while owner alive (no dual epoch); **shared mirror + promote** (Phase 138): best-effort peer MirrorPut/Delete (opcodes 90–93); foreign mirror not served while owner up; owner death / forward fail → promote mirror into primary under same session_id and serve locally when present, else **70**; **mirror polish** (Phase 139): coalesce dirty ops (one per session_id; Delete supersedes Put); debounced Puts (`VOLANT_FETCH_SESSION_MIRROR_PUT_MIN_INTERVAL_MS` default 50; Delete immediate); optional durable `{data_dir}/__fetch_session_mirrors` (`VOLANT_FETCH_SESSION_MIRROR_DURABLE`); `mirror_gen` fencing on apply/promote; **promote claim fence** (Phase 143): `promoted_by` lowest-id on equal-fresh dual-promote via MirrorPut; metric `volant_fetch_session_promote_claim_reject_total`; not Raft / not controller SoT; put lag/fail still **70**; brief dual primary until claim exchange; no re-encode of session_id owner bits; no serve-from-mirror without promote; **PreferredReadReplica** → see Preferred replica row (126+133+140+144) |
| Preferred replica | **MVP** (Phase **126** + **133** + **140** + **144**): Fetch v11+ rack → same-rack live ISR peer with usable addr + LEO≥HWM redirect (empty records); **rank highest LEO then lowest id** (133); optional `VOLANT_PREFERRED_REPLICA_MAX_LEO_LAG` skips peers over lag vs leader LEO (**unset** = unlimited) (140); **suppressed when isolation=READ_COMMITTED** (leader serves aborted filter); metric `volant_preferred_replica_suppressed_total` when a candidate existed (140); **suppressed when client has established fetch session** (`req_session_id != 0`, non-FINAL) to avoid session-owner thrash after preferred redirect; metric `volant_preferred_replica_session_suppressed_total` (144); full fetch `session_id == 0` may still redirect; gated off for followers (`replica_id` / ReplicaState); Metadata rack from `cluster.toml`; not full Kafka selector/throttling |
| Rack-aware assignment | **MVP** (Phase **145**): create-topic / create-partitions maximize rack diversity when ≥2 distinct configured racks; legacy round-robin when no/single rack or `VOLANT_RACK_AWARE_ASSIGNMENT=0`; metric `volant_rack_aware_assignment_total`; no rebalance of existing topics |
| Fetch sessions | **Real MVP** (Phase 88 + **91** + **95** + **97** + **99** + **100** + **102** + **103** + **115** + **119** + **126** + **138** + **139** + **143** + **144** + **147**): create/merge/forgotten; empty-topics re-fetch; errors 70/71; **omit-unchanged** when HWM+LSO unchanged and records empty; **idle TTL** (default 60s / `VOLANT_FETCH_SESSION_IDLE_MS`) + **max sessions** (default 1000 / `VOLANT_FETCH_SESSION_MAX`, LRU at cap); idle also background-swept (Phase 97); knobs on BROKER Describe/Alter (Phase 99) with **sparse** durable restart (Phase 100/102) and name vs `node_id` (Phase 103); **session table durable** under `{data_dir}/__fetch_sessions` (Phase 115); **multi-broker handoff** (Phase 119): cluster session_id embeds owner; non-owner transparent-forwards Fetch to owner over inter-broker while owner alive (no dual epoch); **shared mirror** (Phase 138): best-effort peer MirrorPut/Delete (opcodes 90–93); foreign mirror not served while owner up; **serve-from-mirror without promote** (Phase 147 default): owner death / forward fail → serve foreign mirror in-place when present (metric `volant_fetch_session_serve_from_mirror_total`; no primary insert); promote opt-in via `VOLANT_FETCH_SESSION_PROMOTE_ON_MISS=1` or legacy `VOLANT_FETCH_SESSION_SERVE_MIRROR_WITHOUT_PROMOTE=0`; else **70**; **mirror polish** (Phase 139): coalesce dirty ops; debounced Puts; optional durable `__fetch_session_mirrors`; `mirror_gen` fence; **promote claim fence** (Phase 143): `promoted_by` lowest-id on equal-fresh dual-promote; dual-epoch residual when two peers both serve mirrors without single SoT; not Raft / not controller SoT; put lag/fail still **70**; no re-encode of session_id owner bits; **PreferredReadReplica** → see Preferred replica row (126+133+140+144) |
| Preferred replica | **MVP** (Phase **126** + **133** + **140** + **144**): Fetch v11+ rack → same-rack live ISR peer with usable addr + LEO≥HWM redirect (empty records); **rank highest LEO then lowest id** (133); optional `VOLANT_PREFERRED_REPLICA_MAX_LEO_LAG` skips peers over lag vs leader LEO (**unset** = unlimited) (140); **suppressed when isolation=READ_COMMITTED** (leader serves aborted filter); metric `volant_preferred_replica_suppressed_total` when a candidate existed (140); **suppressed when client has established fetch session** (`req_session_id != 0`, non-FINAL) to avoid session-owner thrash after preferred redirect; metric `volant_preferred_replica_session_suppressed_total` (144); full fetch `session_id == 0` may still redirect; gated off for followers (`replica_id` / ReplicaState); Metadata rack from `cluster.toml`; not full Kafka selector/throttling / rack-aware partition assignment |

| ISR / HWM (cluster) | Kafka-style static ISR (Phase 6): death shrink + HWM recompute (Phase 108/110); **rejoin** when ReplicaFetch LEO ≥ HWM and lag ≤ `replica_lag_max_messages`; lag-shrink of slow-but-alive members (Phase 118); **time-based lag shrink** via last-caught-up + `replica_lag_max_ms` (Phase 125; not full Kafka `replica.lag.time.max.ms` parity); **Phase 142:** Metadata on the leader overlays local ISR; non-controller leaders best-effort `IsrUpdate` (94/95) to controller — non-leader Metadata may still lag until report + ClusterState |
| CreateTopics | Replica assignment arrays ignored; configs response often null; wait/rollback same as native CreateTopic (majority miss → **19**) |
| Storage | Log stores uncompressed Volant records; Fetch re-encodes |
| Auth | Kafka port: SASL or `kafka-anonymous`; no shared-token on Kafka port |
| ACLs | LITERAL only; host always `*`; User resource (v3) stored only (no SCRAM-admin gating); **TransactionalId** (Kafka **5** / native **4**) consulted as Write on txn APIs when ACLs are on and `transactional_id` is non-empty (v0.247); no DelegationToken resource type; **CreateDelegationToken** (key **38**) advertised as a Cluster ALTER reject (v0.258); **RenewDelegationToken** (key **39**) advertised as reject (v0.261); **ExpireDelegationToken** (key **40**) advertised as reject (v0.260); **DescribeDelegationToken** (key **41**) advertised empty (v0.259); cluster Create/Delete are **controller-only** with generationed snapshot fan-out (Phase 113) + durable-gen rejoin catch-up (Phase 117) — not Raft multi-master consensus |
| BROKER config (cluster) | Alter / IncrementalAlter for the six Phase 99 knobs: **controller-only** (Kafka **41** NotController elsewhere); push to live peers; Describe on any node returns **local** effective values after apply; offline peers catch up on heartbeat re-push (Phase 117; not Raft) |
| DeleteRecords (cluster) | Leader-only client path; best-effort inter-broker truncate of other replicas (Phase 113); fan-out failure does not fail the client by default; failed peers recorded in leader-local durable outbox and retried when live (Phase 116); **Phase 123** new leader rebuilds pending targets from local `log_start` after leadership change; truncate journal SoT + majority (Phase 129/130) with optional client wait (Phase 135 env; Phase 137 **native** per-request trailer only — Kafka still broker knob); local low not rolled back on wait fail; **Phase 137** assignment prune + known-topic push filter (journal GC / anti-resurrection) — still not a full Raft truncate log |
| KIP-951 | CurrentLeader on leader errors; Produce NodeEndpoints v10+; Fetch NodeEndpoints v16+; empty tags on success |
| ApiVersions features | Empty SupportedFeatures / FinalizedFeatures / ZkMigrationReady tags; no REBOOTSTRAP_REQUIRED |
| ElectLeaders | **v0–1 wrap** (v0.236): key **43** advertised; preferred = `elect_leader(ISR∩live)`; ElectionType **1** unclean refused (**87**); TimeoutMs ignored; assignment wait/rollback same as key 45; not Kafka `preferred.leader`; not live replica copy |
| AlterPartitionReassignments | **v0 wrap** (v0.225): key **45** advertised; apply is native opcode 114 (instant; new replicas start empty); TimeoutMs ignored; `replicas=null` → **83** (no cancel log / no pending state); not live Kafka reassignment |
| ListPartitionReassignments | **v0 list** (v0.228): key **46** advertised; current assignment as `replicas`; empty adding/removing (apply is instant; no pending log); TimeoutMs ignored; not live Kafka reassignment progress |
| Describe/AlterUserScramCredentials | **v0 wrap** (v0.233): keys **50** / **51** advertised; wrap `ScramStore` (native 64–69). Alter upsert is Kafka `saltedPassword = Hi(...)`, not plaintext. Native create still sends password in the clear. Unknown user → **91** `RESOURCE_NOT_FOUND`. Not OAUTH/GSSAPI |
| Describe/AlterClientQuotas | **v0 honest empty/reject** (v0.241): keys **48** / **49** advertised. No quota store. Describe any filter → error **0**, empty entries. Alter per-entry **42** (`quotas not supported`); `validateOnly` still rejected; nothing persisted. ACL Cluster DESCRIBE / ALTER. v1+ → **35**. Official Kafka 0–1; Volant v0 only |
| DescribeTopicPartitions | **v0 wrap** (v0.237): key **75** advertised; wraps `Broker::metadata`. Same leaders / ISR / epochs / deterministic TopicId as Metadata. Unknown topic → **3**. ACL Topic DESCRIBE. Empty topics = all. `responsePartitionLimit <= 0` unlimited; simple truncate + `next_cursor` when cut. Cursor honored only when its topic is in the result set (else ignored). No ELR fields (Metadata partition body reused). Not Metadata v13+ |
| BrokerRegistration | **v0 reject** (v0.263): key **62** advertised (always flex; official Kafka BrokerRegistration). Not KRaft (no incarnation / DirectoryId / features). Parse official v0 body and reject **42** `INVALID_REQUEST`. Does **not** wrap native AddBroker. Overlay `membership.json` / broker list unchanged (`brokerEpoch` **-1**). Controller not required. ACL Cluster ALTER (deny → **31**). v1+ → **35**. BrokerHeartbeat (**63**) is advertised as reject (v0.265) |
| BrokerHeartbeat | **v0 reject** (v0.265): key **63** advertised (always flex; official Kafka BrokerHeartbeat). Not KRaft (no fencing / metadata offset / assigned epoch). Parse official v0 body and reject **42** `INVALID_REQUEST`. Does **not** wrap native Heartbeat 12 or `GroupCoordinator::heartbeat`. Overlay `membership.json` / broker list unchanged. Controller not required. ACL Cluster ALTER (deny → **31**). Official defaults: `isCaughtUp` false, `isFenced` true, `shouldShutDown` false. No `errorMessage`. v1+ → **35**. Official validVersions 0–2; Volant advertises 0 only |
| ControllerRegistration | **v0 reject** (v0.268): key **70** advertised (always flex; official Kafka ControllerRegistration). Not a KRaft controller (no incarnation / ZK migration / controller listener store). Parse official v0 body and reject **42** `INVALID_REQUEST` (`not KRaft controller registration`). Does **not** wrap native AddBroker. Overlay `membership.json` / broker list unchanged. Controller not required. ACL Cluster ALTER (deny → **31**, errorMessage null). Official validVersions is 0 only; Volant v0 is flexible (official `flexibleVersions: 0+`). v1+ → **35** |
| AddRaftVoter | **v0 reject** (v0.271): key **80** advertised (always flex; official Kafka AddRaftVoter). Not a KRaft raft voter (no voter set / DirectoryId store). Parse official v0 body and reject **42** `INVALID_REQUEST` (`not KRaft raft voter`). Does **not** wrap native AddBroker. Overlay `membership.json` / broker list unchanged. Controller not required. ACL Cluster ALTER (deny → **31**, errorMessage null). Official validVersions 0–1; Volant advertises 0 only as flexible (official `flexibleVersions: 0+`). AckWhenCommitted is v1+ and is not parsed. v1+ → **35** |
| UpdateRaftVoter | **v0 reject** (v0.273): key **82** advertised (always flex; official Kafka UpdateRaftVoter). Not a KRaft voter set (no DirectoryId / listener / KRaftVersionFeature store). Parse official v0 body and reject **42** `INVALID_REQUEST` (`not KRaft raft voter`). Overlay `membership.json` / broker list unchanged. Controller not required. ACL Cluster ALTER (deny → **31**). Official response has no `errorMessage`; CurrentLeader tag 0 is omitted (empty tags). Official validVersions is 0 only; Volant v0 is flexible (official `flexibleVersions: 0+`). v1+ → **35** |
| UnregisterController | **v0 reject** (v0.274): key **94** advertised (always flex; official Kafka UnregisterController). Not a KRaft controller (no unregister record). Parse official v0 body and reject **42** `INVALID_REQUEST` (`not KRaft controller unregister`). Does **not** wrap native `remove_broker`. Overlay `membership.json` / broker list unchanged. Controller not required. ACL Cluster ALTER (deny → **31**, errorMessage null). Official validVersions is 0 only; Volant v0 is flexible (official `flexibleVersions: 0+`). v1+ → **35**. ControllerRegistration **70** remains the reject sibling |
| UnregisterBroker | **v0 wrap** (v0.242): key **64** advertised; wraps native `remove_broker` (same invert as AddBroker / v0.217). Not KRaft UnregisterBroker (no incarnation / DirectoryId). Controller only (**41**); no cluster → **42** “unregister requires cluster”; self / last broker map native InvalidArgument → **42**. TimeoutMs parsed if present before tags, ignored. ACL Cluster ALTER |
| RemoveRaftVoter | **v0 reject** (v0.272): key **81** advertised (always flex; official Kafka RemoveRaftVoter). Not a KRaft controller (no voter set / DirectoryId). Parse official v0 body and reject **42** `INVALID_REQUEST` (`not KRaft raft voter`). Does **not** wrap native `remove_broker`. Overlay `membership.json` / broker list unchanged. UnregisterBroker **64** remains the invert of native remove. Controller not required. ACL Cluster ALTER (deny → **31**, errorMessage null). Official validVersions is 0 only; Volant v0 is flexible (official `flexibleVersions: 0+`). v1+ → **35**. AddRaftVoter **80** / UpdateRaftVoter **82** are advertised as reject (v0.271 / v0.273) |
| Envelope | **v0 reject** (v0.266): key **58** advertised (always flex; official Kafka Envelope). No request forwarding (not KIP-590). Parse official v0 body (`RequestData`, `RequestPrincipal`, `ClientHostAddress`) and reject **42** `INVALID_REQUEST` (`forwarding not supported`). Embedded RequestData is discarded — not unwrapped / not executed. Official response has **no throttleTimeMs**: `ResponseData` **null**, error **42**. Controller not required. ACL Cluster ALTER (deny → **31**). v1+ → **35**. FetchSnapshot (**59**) is advertised as reject (v0.267) |
| UpdateFeatures | **v0–1 reject** (v0.244): key **57** advertised (always flex). Parse request; every feature → **92** `FEATURE_UPDATE_FAILED` (`empty / not supported`). Does **not** persist. ApiVersions SupportedFeatures / FinalizedFeatures stay empty. Not KIP-584. Cluster ALTER. Cluster non-controller → **41**. v2+ → **35**. Describe is already empty via ApiVersions |
| FetchSnapshot | **v0 reject** (v0.267): key **59** advertised (always flex; official Kafka FetchSnapshot). Not a KRaft controller (no metadata snapshot bytes). Parse official v0 body and reject **42** `INVALID_REQUEST`. Empty `topics[]`. Does **not** wrap native InstallSnapshot 112/113. Controller not required. ACL Cluster DESCRIBE (deny → **31**). v1+ → **35**. Envelope (**58**) is advertised as reject (v0.266) |
| Vote | **v0 reject** (v0.270): key **52** advertised (always flex; official Kafka Vote). Not a KRaft controller. Parse official v0 body and reject **42** `INVALID_REQUEST` (`not KRaft vote`). Empty `topics[]`. Does **not** wrap openraft RequestVote or native vote. No vote granted, no PreVote, no voter keys. Official response has **no throttleTimeMs**. Controller not required. ACL Cluster ALTER (deny → **31**). v1+ → **35**. Official validVersions 0–2; Volant advertises 0 only. DescribeQuorum (**55**) unchanged |
| BeginQuorumEpoch | **v1 reject** (v0.289): key **53** advertised (always flex; official Kafka BeginQuorumEpoch). Not a KRaft quorum. Parse official v1 body and reject **42** `INVALID_REQUEST` (`not KRaft quorum epoch`). Empty `topics[]`. Does **not** wrap openraft RequestVote or Vote **52**. Overlay membership unchanged. Official response has **no throttleTimeMs**. NodeEndpoints is v1+ tag 0; unused. Controller not required. ACL Cluster ALTER (deny → **31**). Official v0 is classic (`flexibleVersions: 1+`) — **not advertised**. v0 and v2+ → **35**. Official validVersions 0–1; Volant advertises 1 only. Vote (**52**) / DescribeQuorum (**55**) unchanged. EndQuorumEpoch (**54**) not advertised |
| DescribeQuorum | **v0–1 wrap** (v0.245): key **55** advertised (always flex). Wraps `openraft_leader_id` / `openraft_term` / `openraft_voter_ids`. Not KRaft `__cluster_metadata` (no invented metadata topic). Empty request topics → one synthetic cluster partition **0** (empty name) when raft is started. Raft off / not started → top-level **0**, empty topics. Cluster non-controller → **41**. `logEndOffset` / `highWatermark` = local LEO/HWM if the requested topic exists locally else **0**. `lastFetch` / `lastCaughtUp` = **-1**. No v2 Nodes / DirectoryId. Cluster DESCRIBE. v2+ → **35** |
| AlterPartition | **v0 wrap** (v0.257): key **56** advertised (always flex). Wraps `Broker::apply_leader_isr_update` (same as native opcodes 94/95). Official v0 TopicName + NewIsr `[]int32` (TopicId is v2, LeaderRecoveryState is v1, NewIsrWithEpochs is v3). BrokerEpoch parsed and **ignored**. Not KRaft NewIsrEpoch / ELR / DirectoryId. Controller only in cluster (per-partition **41**); single-node no-op **0**. Native errors: NotFound → **3**, InvalidArg → **42**, NotLeader → **6**, stale epoch → **74**. ACL Cluster ALTER (deny → **31**). v1+ → **35** |
| AllocateProducerIds | **v0 wrap** (v0.246): key **67** advertised (always flex). Block of **1000** from `next_producer_id` (`fetch_add` + persist `__producer_state` like InitProducerId). BrokerEpoch parsed and **ignored**. Not KRaft fencing. Controller only in cluster (**41**); single-node allowed. ACL Cluster ALTER. v1+ → **35** |
| AlterReplicaLogDirs | **v0–1 reject** (v0.249): key **34** advertised. Single `data_dir`. Parse request; every partition **42** `INVALID_REQUEST` (`single data_dir; replica move not supported`). Does **not** move files. Not multi-log.dirs. Controller not required. Official Kafka first flexible is **2**; Volant v1 is flexible. DescribeLogDirs (35) unchanged. ACL Cluster ALTER or Topic ALTER. v2+ → **35** |
| WriteTxnMarkers | **v0–1 wrap** (v0.250): key **27** advertised. Replica-local apply: one COMMIT/ABORT control batch per listed partition (`txn_control_message` / `produce_one` / `flush`) + matching soft `__txn_markers`. CoordinatorEpoch parsed and **ignored**. Does **not** call EndTxn (no coordinator finalize / prepared 2PC). Controller not required. Unknown topic → **3**. ACL Topic WRITE or Cluster ALTER; denied → **29**. v2+ → **35**. Not a transaction coordinator |
| AssignReplicasToDirs | **v0 reject** (v0.251): key **73** advertised (always flex). Single `data_dir`. Parse request; every partition **42** `INVALID_REQUEST` (`single data_dir; directory assignment not supported`). Does **not** move files. Does **not** invent DirectoryId storage. Not KRaft. BrokerEpoch ignored. Controller not required. ACL Cluster ALTER (deny → top-level **31**, empty directories). v1+ → **35** |
| GetTelemetrySubscriptions | **v0 empty** (v0.253): key **71** advertised (always flex). No client telemetry (not KIP-714). Parse request; error **0**, echo `clientInstanceId`, `subscriptionId = 0`, empty `requestedMetrics` / accepted compression, `pushIntervalMs = -1` (do not push), `telemetryMaxBytes = 0`, `deltaTemporality = false`. Nothing persisted. Controller not required. Cluster DESCRIBE. Denied → **31** (still echo id; empty metrics). v1+ → **35**. PushTelemetry (72) is advertised as reject (v0.255) |
| PushTelemetry | **v0 reject** (v0.255): key **72** advertised (always flex). No client telemetry (not KIP-714). Parse request (`clientInstanceId`, `subscriptionId`, `terminating`, `compressionType`, compact `metrics`); discard bytes. Throttle **0**, error **42** `INVALID_REQUEST`. Official body has no `errorMessage`. Nothing persisted. Controller not required. Cluster ALTER. Denied → **31** (still nothing persisted). v1+ → **35**. GetTelemetrySubscriptions stays empty / `pushIntervalMs = -1` |
| CreateDelegationToken | **v0 reject** (v0.258): key **38** advertised (always flex). No delegation-token store. Parse `renewers[]` + `maxLifeTimeMs`; top-level **42** `INVALID_REQUEST` (`delegation tokens not supported`). Empty/zero token fields (`tokenId` empty, `hmac` empty, issue/expiry/max **-1**). Nothing persisted. Controller not required. Official Kafka first flexible is **2**; Volant v0 is flexible. ACL Cluster ALTER (deny → **31**). v1+ → **35**. Renew/Expire advertised as reject (v0.261 / v0.260) |
| RenewDelegationToken | **v0 reject** (v0.261): key **39** advertised (always flex). No delegation-token store. Parse compact `hmac` + `renewPeriodMs`; top-level **42** `INVALID_REQUEST` (`delegation tokens not supported`). Official response: `errorCode`, `expiryTimestampMs` **-1**, `throttleTimeMs` **0** (no errorMessage). Nothing persisted. Controller not required. Official Kafka first flexible is **2**; Volant v0 is flexible. ACL Cluster ALTER (deny → **31**). v1+ → **35** |
| ExpireDelegationToken | **v0 reject** (v0.260): key **40** advertised (always flex). No delegation-token store. Parse compact `hmac` + `expiryTimePeriodMs`; top-level **42** `INVALID_REQUEST` (`delegation tokens not supported`). Official response: `errorCode`, `expiryTimestampMs` **-1**, `throttleTimeMs` **0** (no errorMessage). Nothing persisted. Controller not required. Official Kafka first flexible is **2**; Volant v0 is flexible. ACL Cluster ALTER (deny → **31**). v1+ → **35** |
| DescribeDelegationToken | **v0 empty** (v0.259): key **41** advertised (always flex residual; official first flex is **2**). No delegation-token store. Parse owners filter (null = all); error **0**, empty tokens. Official response order: `errorCode`, `tokens[]`, `throttleTimeMs` (no errorMessage). Nothing persisted. Controller not required. Cluster DESCRIBE. Denied → **31**, empty tokens. v1+ → **35**. Renew/Expire advertised as reject (v0.261 / v0.260) |
| ConsumerGroupDescribe | **v0 wrap** (v0.264): key **69** advertised (always flex). Wraps `GroupCoordinator::describe_group` — same snapshot as DescribeGroups **15**. **Not KIP-848**: `memberEpoch` **-1**, no `targetMemberEpoch`, no regex subscribe, no consumer-protocol assignor streams. Official v0 has no GroupType field (ListGroups v5 still says `classic`). `groupEpoch` / `assignmentEpoch` = generation. `assignorName` = `range` when members exist else empty. Unknown group → per-group **69** `GROUP_ID_NOT_FOUND` (same code DescribeGroups uses; official 69 also uses **69**). Offset-only empty group → error **0**, `Empty` (same as key 15). `includeAuthorizedOperations` true → **0** (no invented ACL bits); false → INT32_MIN. Group DESCRIBE per id; denied → **30**. Controller not required. Official Kafka 0–1 (v1 adds MemberType); Volant v0 only. v1+ → **35**. ConsumerGroupHeartbeat (**68**) advertised as reject (v0.269) |
| ConsumerGroupHeartbeat | **v0 reject** (v0.269): key **68** advertised (always flex). **Not KIP-848**. Parse official v0 body and reject **42** `INVALID_REQUEST` (`not KIP-848 consumer protocol`). Does **not** wrap classic Heartbeat **12** / `GroupCoordinator::heartbeat`. Does not join / leave / assign. Group membership unchanged. `memberId` null, `memberEpoch` **-1**, `heartbeatIntervalMs` **0**, assignment null. Controller not required. Group READ (deny → **30**, errorMessage null). Official Kafka 0–1 (v1 adds SubscribedTopicRegex); Volant v0 only. v1+ → **35** |
| ShareGroupDescribe | **v1 reject** (v0.276): key **77** advertised (always flex). **Not KIP-932**. Official `validVersions` is **1** only (v0 EA in Kafka 4.0, removed in 4.1). Parse official v1 body and reject each group **42** `INVALID_REQUEST` (`not KIP-932 share group`). Official response has **no top-level error**. Empty members; empty `groupState`; `groupEpoch` / `assignmentEpoch` **-1**; `assignorName` empty. Does **not** wrap `describe_group` / ConsumerGroupDescribe **69** / DescribeGroups **15**. `includeAuthorizedOperations` true → **0** (no invented ACL bits); false → INT32_MIN. Group DESCRIBE per id; denied → **30**, errorMessage null. Controller not required. v0 and v2+ → **35** |
| ShareFetch | **v1 reject** (v0.277): key **78** advertised (always flex). **Not KIP-932**. Parse official v1 body and reject **42** `INVALID_REQUEST` (`not KIP-932 share fetch`). Does **not** wrap Kafka Fetch **1** or native Fetch. Does not acquire records or create a share session. Empty `Responses[]` / `NodeEndpoints[]`. `acquisitionLockTimeoutMs` **0**. Controller not required. Group READ (deny → **30**, errorMessage null). Official Kafka 1–2 (v0 EA removed in 4.1; v2 adds ShareAcquireMode / Renew); Volant v1 only. v0 and v2+ → **35** |
| ShareAcknowledge | **v1 reject** (v0.278): key **79** advertised (always flex). **Not KIP-932**. Parse official v1 body and reject **42** `INVALID_REQUEST` (`not KIP-932 share acknowledge`). Does **not** wrap OffsetCommit **8** / Fetch **1**. Does not mutate offsets or record state. Empty `Responses[]` / `NodeEndpoints[]`. Controller not required. Group READ (deny → **30**, errorMessage null). Official Kafka 1–2 (v0 removed in 4.1; v2 adds IsRenewAck / AcquisitionLockTimeoutMs); Volant v1 only. v0 and v2+ → **35** |
| InitializeShareGroupState | **v0 reject** (v0.279): key **83** advertised (always flex). **Not KIP-932**. Parse official v0 body and reject per-partition **42** `INVALID_REQUEST` (`not KIP-932 share state`). Official response has **no throttleTimeMs** and **no top-level error** — echo TopicId + Partition. Unparseable body → empty `Results[]`. Does **not** persist share state. Does **not** wrap OffsetCommit. Controller not required. Group ALTER (deny → per-partition **30**, errorMessage null). Official validVersions is 0 only; Volant v0 is flexible (official `flexibleVersions: 0+`). v1+ → **35** |
| ReadShareGroupState | **v0 reject** (v0.280): key **84** advertised (always flex). **Not KIP-932**. Parse official v0 body and reject per-partition **42** `INVALID_REQUEST` (`not KIP-932 share state`). Official response has **no throttleTimeMs** and **no top-level error** — echo TopicId + Partition with StateEpoch **0**, StartOffset **-1**, empty `StateBatches[]`. Unparseable body → empty `Results[]`. Does **not** persist share state. Does **not** wrap OffsetFetch / OffsetCommit / InitializeShareGroupState. Controller not required. Group READ (deny → per-partition **30**, errorMessage null). Official validVersions is 0 only; Volant v0 is flexible (official `flexibleVersions: 0+`). v1+ → **35** |
| WriteShareGroupState | **v0 reject** (v0.281): key **85** advertised (always flex). **Not KIP-932**. Parse official v0 body and reject per-partition **42** `INVALID_REQUEST` (`not KIP-932 share state`). Official response has **no throttleTimeMs** and **no top-level error** — echo TopicId + Partition. Unparseable body → empty `Results[]`. Does **not** persist share state. Does **not** wrap OffsetCommit **8** / InitializeShareGroupState **83**. Controller not required. Group ALTER (deny → per-partition **30**, errorMessage null). Official validVersions is **0–1** (v1 adds DeliveryCompleteCount / KIP-1226); Volant advertises v0 only as flexible (official `flexibleVersions: 0+`). v1+ → **35** |
| DeleteShareGroupState | **v0 reject** (v0.282): key **86** advertised (always flex). **Not KIP-932**. Parse official v0 body and reject per-partition **42** `INVALID_REQUEST` (`not KIP-932 share state`). Official response has **no throttleTimeMs** and **no top-level error** — echo TopicId + Partition. Unparseable body → empty `Results[]`. Does **not** persist share state. Does **not** wrap OffsetCommit **8** / DeleteGroups **42** / InitializeShareGroupState **83**. Controller not required. Group ALTER (deny → per-partition **30**, errorMessage null). Official validVersions is 0 only; Volant v0 is flexible (official `flexibleVersions: 0+`). Official request has no StateEpoch / StartOffset (unlike key 83). v1+ → **35** |
| ReadShareGroupStateSummary | **v0 reject** (v0.283): key **87** advertised (always flex). **Not KIP-932**. Parse official v0 body (same shape as ReadShareGroupState **84**) and reject per-partition **42** `INVALID_REQUEST` (`not KIP-932 share state`). Official response has **no throttleTimeMs** and **no top-level error** — echo TopicId + Partition with StateEpoch **0**, LeaderEpoch **-1**, StartOffset **-1**. Unparseable body → empty `Results[]`. Does **not** write DeliveryCompleteCount (official v1 / KIP-1226). Does **not** persist share state. Does **not** wrap OffsetFetch / OffsetCommit / InitializeShareGroupState / ReadShareGroupState. Controller not required. Group READ (deny → per-partition **30**, errorMessage null). Official validVersions is 0–1; Volant advertises v0 only as flexible (official `flexibleVersions: 0+`). v1+ → **35** |
| DescribeShareGroupOffsets | **v0 reject** (v0.284): key **90** advertised (always flex). **Not KIP-932**. Official `validVersions` is **0–1** (v1 adds Lag / KIP-1226); Volant advertises **0** only. Parse official v0 body (group ids; skip topics/partitions) and reject each group **42** `INVALID_REQUEST` (`not KIP-932 share offsets`). Official response **has** `throttleTimeMs` and **per-group** error **after** empty `Topics[]` (no top-level error besides throttle). Unparseable body → throttle **0** + empty `Groups[]`. Does **not** persist. Does **not** wrap OffsetFetch **9** / `describe_group` / ConsumerGroupDescribe **69** / ShareGroupDescribe **77**. Do **not** write Lag. Group DESCRIBE per id; denied → **30**, errorMessage null, empty topics. Controller not required. v1+ → **35** |
| StreamsGroupHeartbeat | **v0 reject** (v0.285): key **88** advertised (always flex). **Not KIP-1071**. Official `validVersions` is **0–1** (v1 adds TopologyDescriptionRequired / KIP-1331); Volant advertises **0** only. Parse official v0 body (`GroupId` for ACL; discard MemberId / MemberEpoch / Topology / tasks / tags) and reject **42** `INVALID_REQUEST` (`not KIP-1071 streams group`). Does **not** wrap classic Heartbeat **12** / ConsumerGroupHeartbeat **68** / ShareGroupHeartbeat **76** / `GroupCoordinator::heartbeat`. Does not join / leave / assign / persist topology. Group membership unchanged. Official v0 response (no `Assignment`): `memberId` null, `memberEpoch` **-1**, `heartbeatIntervalMs` **0**, `AcceptableRecoveryLagLegacy` **0**, remaining official v0 fields empty/null/0. Do **not** write TopologyDescriptionRequired or AcceptableRecoveryLag (v1+). Controller not required. Group READ (deny → **30**, errorMessage null). v1+ → **35** |
| StreamsGroupDescribe | **v0 reject** (v0.286): key **89** advertised (always flex). **Not KIP-1071**. Official `validVersions` is **0–1** (v1 adds IncludeTopologyDescription / KIP-1331); Volant advertises **0** only. Parse official v0 body and reject each group **42** `INVALID_REQUEST` (`not KIP-1071 streams group`). Official response has **no top-level error** besides throttle. Empty members; Topology **null** (official: null on describe error); empty `groupState`; `groupEpoch` / `assignmentEpoch` **-1**. Does **not** wrap `describe_group` / ConsumerGroupDescribe **69** / DescribeGroups **15** / ShareGroupDescribe **77**. `includeAuthorizedOperations` true → **0** (no invented ACL bits); false → INT32_MIN. Group DESCRIBE per id; denied → **30**, errorMessage null. Controller not required. v1+ → **35** |
| AlterShareGroupOffsets | **v0 reject** (v0.287): key **91** advertised (always flex). **Not KIP-932**. Official `validVersions` is **0** only. Parse official v0 body (GroupId for ACL; echo topic/partition names; discard StartOffset) and reject **42** `INVALID_REQUEST` (`not KIP-932 share offsets`). Official response **has** `throttleTimeMs`, **top-level** ErrorCode / ErrorMessage, **and** per-topic/per-partition errors. TopicId is the zero UUID (request is name-only). Unparseable body → throttle **0** + top-level **42** + empty `Responses[]`. Does **not** persist. Does **not** wrap OffsetCommit **8** / DescribeShareGroupOffsets **90**. Group ALTER on groupId; denied → **30**, errorMessage null. Controller not required. v1+ → **35** |
| DeleteShareGroupOffsets | **v0 reject** (v0.288): key **92** advertised (always flex). **Not KIP-932**. Official `validVersions` is **0** only. Parse official v0 body (GroupId + topic names only; no partitions) and reject **42** `INVALID_REQUEST` (`not KIP-932 share offsets`) at **top-level and per-topic**. Official response **has** `throttleTimeMs`, **top-level** ErrorCode/ErrorMessage, and per-topic `Responses[]` (TopicName, TopicId, ErrorCode, ErrorMessage). Echo parsed topic names with TopicId **zero**. Unparseable body → throttle **0** + top-level **42** + empty `Responses[]` (not success). Does **not** persist. Does **not** wrap OffsetCommit **8** / DeleteGroups **42** / OffsetDelete **47** / DescribeShareGroupOffsets **90**. Group ALTER on groupId; denied → **30**, errorMessage null. Controller not required. v1+ → **35** |
| Missing APIs | Large Kafka surface still unsupported (GSSAPI, OAUTH, …) |

## Related

- [ops.md](./ops.md) — Kafka listen ops notes  
- [features.md](./features.md) — native txn/ACL/SCRAM behavior  
- [history/PHASE_HISTORY.md](./history/PHASE_HISTORY.md) — phase index  
